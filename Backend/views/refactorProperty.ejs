<!DOCTYPE html>
<html>

<head>
    <title>Property</title>
    <meta charset="UTF-8">
    <link rel="stylesheet" type="text/css" href="/stylesheets/styleRegister.css" />
    <link rel="shortcut icon" href="/images/logo.png" type="image/x-icon" />
</head>

<body>
    <header>
        <div class="logo">
            <img src="/images/logo.png" alt="Logo" />
            <h1>Henrique & Angelo</h1>
        </div>
        <nav>
            <a href="/api/logout">Logout</a>
            <a href="/api/property">Property</a>
            <a href="/api/event">Events</a>
            <a href="/api/ticket">Tickets</a>
            <a href="/api/client">Costumer</a>
            <a href = "/api/updateDiscount">Discounts</a>
        </nav>
    </header>


    <div class="container">
        <h1>Management Property</h1>
        <div>
            <h2>Update Property</h2>
            <form method="POST" action="/api/refactorProperty/<%= property._id %>">
                <label for="propertyName">Name:</label>
                <input type="text" name="propertyName" id="propertyName" value="<%= property.propertyName %>" />

                <label for="address">Address:</label>
                <input type="text" name="address" id="address" value="<%= property.address %>" />

                <label for="description">Description:</label>
                <textarea name="description" id="description"><%= property.description %></textarea>

                <label for="capacity">Capacity: </label>
                <input type="number" name="capacity" id="capacity" value="<%= property.capacity %>" min="1" required />

                <% if ( typeof message !=='undefined' && message) { %>
                    <p>
                        <%= message %>
                    </p>
                    <% } %>

                        <button type="submit"> Update Property </button>
            </form>


            <button href="/api/home">Back to home</button>
        </div>
    </div>