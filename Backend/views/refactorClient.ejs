<!DOCTYPE html>
<html>

<head>
    <title>Property</title>
    <meta charset="UTF-8">
    <link rel="stylesheet" type="text/css" href="/stylesheets/styleRegister.css" />
    <link rel="shortcut icon" href="/images/logo.png" type="image/x-icon" />
</head>

<body>
    <header>
        <div class="logo">
            <img src="/images/logo.png" alt="Logo" />
            <h1>Henrique & Angelo</h1>
        </div>
        <nav>
            <a href="/api/logout">Logout</a>
            <a href="/api/property">Property</a>
            <a href="/api/event">Events</a>
            <a href="/api/ticket">Tickets</a>
            <a href="/api/client">Costumer</a>
            <a href = "/api/updateDiscount">Discounts</a>
        </nav>
    </header>

    <div class="container">
        <h1>Management Costumer</h1>
        <div>
            <h2>Update Costumer</h2>
            <form method="POST" action="/api/refactorClient/<%= client._id %>">
                <label for="firstName">First Name:</label>
                <input type="text" name="firstName" id="firstName" required value="<%= client.firstName %>" />

                <label for="lastName">Last Name:</label>
                <input type="text" name="lastName" id="lastName" required value="<%= client.lastName %>" />

                <label for="emailClient">Email:</label>
                <input type="email" name="emailClient" id="emailClient" value="<%= client.emailClient %>" required>

                <label for="contact">Phone Number:</label>
                <input type="tel" name="contact" id="contact" value="<%= client.contact %>" required pattern="\d{9}" />

                <label for="nif">NIF:</label>
                <input type="text" id="nif" name="nif" value="<%= client.nif %>" required pattern="\d{9}">
                <% if (typeof message !=='undefined' && message) { %>
                    <p>
                        <%= message %>
                    </p>
                    <% } %>
                        <button type="submit">Update Costumer</button>

            </form>

            <button><a href="/api/client/" style="text-decoration:none;">Back to Costumer</a></button>
        </div>
    </div>
</body>

</html>